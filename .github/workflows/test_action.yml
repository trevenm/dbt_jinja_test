name: test_action

on:
  schedule:
    - cron: '0-59/5 * * * *'
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run a one line script
        run: echo Hello, world!